package org.duhemm.parsermacro

/**
 * Attachment that represents the arguments of parser macro.
 * Each separate parser macro argument is represented as an element of `args`.
 */
case class ParserMacroArgumentsAttachment(args: List[String])
